# NUSENSE Virtual Try-On

This is a Vite + React project that renders the Virtual Try-On modal.

## Run locally

1. Install dependencies:
   - npm install
2. Start the dev server:
   - npm run dev

Then open the local URL shown in the terminal.